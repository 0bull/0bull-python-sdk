# Examples

Runnable scripts demonstrating the SDK. Set `ZEROBULL_API_TOKEN` in your environment (or pass
`api_token=...` to the client), then run any script with `uv run python examples/<script>.py`;
`upload_video.py` additionally takes a local video path as its argument.
